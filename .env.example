# ============================================================================
# 环境变量示例
# ----------------------------------------------------------------------------
# 不配置时，系统使用内置 mock Embedding 与 mock（抽取式）LLM，可完整跑通主链路。
# 配置以下变量后，自动切换为真实服务（见 lib/ai/embedding.ts、lib/ai/llm.ts）。
# 复制本文件为 .env.local 并填入真实值。
#
# Provider 优先级：智谱 GLM > 通用 OpenAI 兼容端点 > 内置 mock。
# ============================================================================

# ---- 智谱 GLM（推荐，开放平台 v4，对话 + 向量同一把 Key） ----
# 只需填 ZHIPU_API_KEY 即可同时启用智谱对话与向量检索。
# ZHIPU_API_KEY=
# ZHIPU_LLM_MODEL=glm-4.6
# ZHIPU_EMBEDDING_MODEL=embedding-3
# ZHIPU_API_URL=https://open.bigmodel.cn/api/paas/v4/chat/completions
# ZHIPU_EMBEDDING_API_URL=https://open.bigmodel.cn/api/paas/v4/embeddings

# ---- Embedding（OpenAI 兼容 /v1/embeddings 接口） ----
# EMBEDDING_API_URL=https://api.openai.com/v1/embeddings
# EMBEDDING_API_KEY=sk-xxxx
# EMBEDDING_MODEL=text-embedding-3-small

# ---- LLM（OpenAI 兼容 /v1/chat/completions 接口） ----
# LLM_API_URL=https://api.openai.com/v1/chat/completions
# LLM_API_KEY=sk-xxxx
# LLM_MODEL=gpt-4o-mini

# ---- 向量库 / 数据库（接入 Supabase 或 PostgreSQL + pgvector 时使用） ----
# DATABASE_URL=postgresql://user:pass@host:5432/dbname
# SUPABASE_URL=
# SUPABASE_ANON_KEY=

# ── 扫描页 OCR（P3，可选，默认关闭）──
# 启用后对扫描页（无文字层、有图像）做 Tesseract OCR，转为可检索段落。
# 依赖：Tesseract-OCR 引擎 + chi_sim 语言包（见 README/memory）。OCR 较慢，按需开启。
# OCR_SCANNED=1
# TESSERACT_CMD=C:\Program Files\Tesseract-OCR\tesseract.exe
# QA_TESSDATA=%LOCALAPPDATA%\qa-tessdata
# OCR_LANG=chi_sim+eng

# ── 扫描页视觉识别（P3 Level 3，可选，默认关闭）──
# 用 GLM-4V 转写扫描页（中文表格质量远高于 Tesseract OCR），需 ZHIPU_API_KEY。
# 优先级高于 OCR_SCANNED；每页一次视觉 API 调用（有成本），按需开启。
# VISION_SCANNED=1
# VISION_MODEL=glm-4v-flash
